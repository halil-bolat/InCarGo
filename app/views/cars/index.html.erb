<p id="notice"><%= notice %></p>

<h1>Welcome to InCarGo</h1>

<br>


<table class="table-bordered">
  <thead>
    <tr>
      <th>Picture</th>
      <th>Brand</th>
      <th>Model</th>
      <th>Year</th>
      <th>Millage</th>
      <th>Price</th>
      <th>Owner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td class="list-img"><%=image_tag("car1.jpg")%></td>
        <td><%= car.brand.brand %></td>
        <td><%= car.model %></td>
        <td><%= car.year %></td>
        <td><%= car.millage %></td>
        <td>£<%= car.price %></td>
        <td><%= car.user.email %></td>
        <td><%= link_to 'Show', car %></td>
        <% if user_signed_in? and car.user_id == current_user.id %><td><%= link_to 'Edit', edit_car_path(car) %></td><%end%>
        <% if user_signed_in? and car.user_id == current_user.id %><td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td><%end%>
      </tr>
    <% end %>
  </tbody>
</table>
